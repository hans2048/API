# 주간 업무 보고 시스템 — 사용설명서

> 접속 URL : `http://서버IP:8000/app_wr`

---

## 목차

1. [시작하기 (로그인)](#1-시작하기-로그인)
2. [화면 구성](#2-화면-구성)
3. [역할(Role) 체계](#3-역할role-체계)
4. [대시보드](#4-대시보드)
5. [주간 보고](#5-주간-보고)
6. [조직도](#6-조직도)
7. [업무 관리 (관리자)](#7-업무-관리-관리자)
8. [조직 관리 (Admin)](#8-조직-관리-admin)
9. [사용자 관리 (Admin)](#9-사용자-관리-admin)
10. [첨부파일 관리 (Admin)](#10-첨부파일-관리-admin)
11. [휴지통 (Admin)](#11-휴지통-admin)
12. [DB 뷰어 (Admin)](#12-db-뷰어-admin)
13. [PPT 내보내기](#13-ppt-내보내기)
14. [계정 설정 (비밀번호 변경)](#14-계정-설정-비밀번호-변경)
15. [환경 변수 (서버 설정)](#15-환경-변수-서버-설정)
16. [FAQ](#16-faq)

---

## 1. 시작하기 (로그인)

### 최초 접속
1. 브라우저에서 `http://서버IP:8000/app_wr` 접속
2. 로그인 화면에서 아이디 · 비밀번호 입력 후 **로그인** 클릭

### 기본 계정 (최초 설치 시)

| 아이디 | 비밀번호 | 역할 |
|--------|----------|------|
| `admin` | `admin1234` | 시스템 관리자 |

> ⚠️ 최초 로그인 후 반드시 비밀번호를 변경하세요. ([→ 14장 참조](#14-계정-설정-비밀번호-변경))

### 로그인 유지
- JWT 토큰 방식으로 인증하며 **12시간** 유지됩니다.
- 만료 후에는 자동으로 로그인 화면으로 이동합니다.

### 회원가입
- 로그인 화면 하단 **회원가입** 링크를 클릭합니다.
- `admin` 역할은 직접 가입할 수 없습니다. (Admin이 계정 생성 필요)

---

## 2. 화면 구성

```
┌─────────────────────────────────────────────────┐
│  🏠 📋 주간 업무 보고 시스템        [로그인 정보] │
├──────────┬──────────────────────────────────────┤
│          │                                      │
│  [사이드  │           메인 콘텐츠 영역            │
│   바 메   │                                      │
│   뉴]    │                                      │
│          │                                      │
└──────────┴──────────────────────────────────────┘
```

### 사이드바 메뉴

| 메뉴 | 접근 권한 | 설명 |
|------|-----------|------|
| 🏠 대시보드 | 전체 | 내 Activity 현황 요약 |
| 📊 주간 보고 | 전체 | 주차별 팀/그룹 보고서 조회 |
| 🗂 조직도 | 전체 | 팀·그룹·사용자 구조 확인 |
| 📌 업무 관리 | 관리자 이상 | 업무 항목 등록·순서 변경 |
| 🏢 조직 관리 | Admin | 팀·그룹 추가·수정·삭제 |
| 👥 사용자 관리 | Admin | 계정 생성·수정·삭제 |
| 📎 첨부파일 관리 | Admin | 첨부파일 조회·삭제·압축 |
| 🗑 휴지통 | Admin | 삭제된 사용자·Activity 복원 |
| 🗄 DB 뷰어 | Admin | 테이블 직접 조회 |

---

## 3. 역할(Role) 체계

| 역할 | 코드 | 주요 권한 |
|------|------|-----------|
| 시스템 관리자 | `admin` | 모든 기능 |
| 팀장 | `team_leader` | 조직·업무 관리, Activity 전체 조회·수정 |
| 그룹장 | `group_leader` | 조직·업무 관리, Activity 전체 조회·수정 |
| 라인장 | `line_leader` | 조직·업무 관리, Activity 전체 조회·수정 |
| 일반 구성원 | `member` | 본인 Activity 등록·수정, 주간보고 조회 |

> - **관리자 이상** = `admin` / `team_leader` / `group_leader` / `line_leader`
> - Activity 입력·수정은 **모든 로그인 사용자**가 가능합니다.

---

## 4. 대시보드

로그인 직후 첫 화면으로 **내 Activity 현황**을 한눈에 확인합니다.

### 기능
- **기간 필터** : From ~ To 날짜를 설정하고 **조회** 클릭
- **상태 필터** : 전체 / 진행중 / 완료 / 예정 선택
- 조건 없이 조회 시 최근 등록된 Activity 목록을 표시

### Activity 상태 색상

| 상태 | 의미 |
|------|------|
| 🟡 진행중 | 현재 진행 중인 업무 |
| 🟢 완료 | 완료된 업무 |
| 🔵 예정 | 향후 예정 업무 |

---

## 5. 주간 보고

팀·그룹별 주차 보고서를 조회하고 Activity를 등록·수정합니다.

### 주차 이동

| 버튼 | 동작 |
|------|------|
| ◀ 이전주 | 한 주 이전으로 이동 |
| 다음주 ▶ | 한 주 이후로 이동 |

- 현재 주차가 상단에 `YYYY-Wnn (날짜 범위)` 형식으로 표시됩니다.

### 팀 · 그룹 필터
- 로그인 시 **본인 소속 팀/그룹이 자동 선택**됩니다.
- 팀 선택박스 변경 시 해당 팀 하위 그룹만 그룹 선택박스에 표시됩니다.
- 필터를 직접 변경하면 이후 수동 선택 상태로 유지됩니다.

### 보고서 구성

각 그룹의 탭을 클릭하면 해당 그룹의 주간 보고 표를 확인합니다.

| 컬럼 | 내용 |
|------|------|
| 업무 | 등록된 업무 항목명 |
| Activity | 세부 활동 내용 |
| 비고 | 리치 텍스트 메모 (서식 지원) |
| 일정 | 예정 일정 |
| 상태 | 진행중 / 완료 / 예정 |
| 담당자 | 배정된 사용자 |

### Activity 추가

1. 보고서 표 하단 **＋ Activity 추가** 버튼 클릭
2. 업무 · Activity명 · 상태 · 일정 · 담당자 · 비고 입력
3. **저장** 클릭

### Activity 수정 · 삭제

- 목록에서 해당 행의 ✏️ 아이콘 클릭 → 수정 모달 열림
- 모달 하단 **삭제** 버튼 → 휴지통으로 이동 (복원 가능)

### 비고 서식 편집

비고 입력란은 리치 텍스트 에디터입니다.

| 버튼 | 기능 |
|------|------|
| **B** | 굵게 |
| *I* | 기울임 |
| <u>U</u> | 밑줄 |
| ~~S~~ | 취소선 |
| 크기 선택 | 작게 / 보통 / 크게 / 매우크게 |
| 색상 선택 | 글자 색상 |
| 서식지우기 | 선택 영역 서식 초기화 |

> PPT 내보내기 시 비고 서식(굵게·색상·크기·밑줄·취소선)이 **그대로 반영**됩니다.

### 전주 Activity 불러오기

Activity 추가·수정 모달 상단 **전주 Activity** 드롭다운에서 전주에 등록한 항목을 선택하면 내용이 자동으로 채워집니다.

### 첨부파일

Activity 모달 하단 첨부파일 영역에서 파일을 업로드합니다.

1. **파일 선택** 버튼 → 파일 선택 (여러 개 가능)
2. **파일 업로드** 클릭
3. 업로드된 파일은 목록에 표시되며 클릭 시 다운로드됩니다.
4. 🗑 아이콘 클릭 → 해당 첨부파일 삭제

> PPT 내보내기 시 첨부파일이 **하이퍼링크**로 삽입됩니다 (클릭 시 파일 다운로드). 사내망에서만 동작합니다.

---

## 6. 조직도

현재 등록된 팀 · 그룹 · 구성원 구조를 카드 형식으로 확인합니다.

- 팀 → 그룹 → 구성원(역할·아이디) 계층 구조로 표시
- 읽기 전용 (수정 불가)

---

## 7. 업무 관리 (관리자)

**접근 권한**: 관리자 이상 (`team_leader` / `group_leader` / `line_leader` / `admin`)

### 업무 등록

1. **그룹 선택** → 해당 그룹의 업무 목록 표시
2. **＋ 업무 추가** 버튼 클릭 → 업무명 입력 후 저장

### 업무 순서 변경 (드래그 앤 드롭)

- 목록 왼쪽 **☰** 아이콘을 드래그하여 순서를 변경합니다.
- 변경 즉시 서버에 저장되며 주간보고 화면과 PPT 내보내기에 반영됩니다.

### 업무 수정 · 삭제

- **수정** 버튼 클릭 → 업무명 변경
- **삭제** 버튼 클릭 → 즉시 삭제 (복원 불가)

---

## 8. 조직 관리 (Admin)

**접근 권한**: `admin`

팀과 그룹을 등록 · 수정 · 삭제합니다.

### 팀 관리

| 작업 | 방법 |
|------|------|
| 팀 추가 | **팀** 탭 → **＋ 팀 추가** 버튼 |
| 팀 수정 | 목록 **수정** 버튼 → 팀명 변경 |
| 팀 삭제 | 목록 **삭제** 버튼 |

### 그룹 관리

| 작업 | 방법 |
|------|------|
| 그룹 추가 | **그룹** 탭 → **＋ 그룹 추가** 버튼 → 그룹명 + 소속 팀 선택 |
| 그룹 수정 | 목록 **수정** 버튼 → 그룹명/팀 변경 |
| 그룹 삭제 | 목록 **삭제** 버튼 |

---

## 9. 사용자 관리 (Admin)

**접근 권한**: `admin`

### 사용자 추가

1. **＋ 사용자 추가** 버튼 클릭
2. 아이디 · 비밀번호 · 이름 · 역할 · 팀 · 그룹 입력
3. **저장** 클릭

> - `admin` 역할의 사용자는 팀/그룹 미지정 가능
> - 나머지 역할은 **팀 선택 필수**

### 사용자 수정

- 목록 **수정** 버튼 → 이름·역할·팀·그룹 변경 가능
- 비밀번호 초기화도 이 화면에서 가능

### 사용자 삭제

- 목록 **삭제** 버튼 → 소프트 삭제 (휴지통으로 이동, 복원 가능)

### 본인 비밀번호 변경

→ [14장 계정 설정](#14-계정-설정-비밀번호-변경) 참조

---

## 10. 첨부파일 관리 (Admin)

**접근 권한**: `admin`

Activity에 업로드된 첨부파일을 전체 조회 · 다운로드 · 삭제합니다.

### 조회

- **등록일(From) ~ (To)** : 날짜 범위 입력 후 **조회** 클릭
- **초기화** 클릭 시 날짜 필터 초기화 후 전체 목록 조회

### 다운로드

- 파일명 링크 클릭 → 개별 다운로드
- 체크박스 선택 후 **선택 다운로드** → 일괄 다운로드

### 삭제

- 체크박스 선택 후 **선택 삭제** → DB에서 삭제
- 개별 행 **삭제** 버튼 → 단건 삭제

> ⚠️ 삭제 후에는 **DB 압축하기** 버튼을 눌러야 실제 파일 용량이 줄어듭니다.

### DB 압축

- **DB 압축하기** 버튼 → SQLite `VACUUM` 실행 → 삭제된 첨부파일 공간 회수

---

## 11. 휴지통 (Admin)

**접근 권한**: `admin`

소프트 삭제된 항목을 복원합니다.

| 탭 | 내용 |
|----|------|
| 삭제된 사용자 | 삭제 처리된 계정 목록 |
| 삭제된 Activity | 삭제 처리된 Activity 목록 |

- **복원** 버튼 클릭 → 원래 상태로 복구

---

## 12. DB 뷰어 (Admin)

**접근 권한**: `admin`

SQLite 테이블 내용을 직접 조회합니다.

- 왼쪽 테이블 목록에서 테이블 클릭 → 데이터 표시
- SQL 입력란에 직접 SELECT 쿼리 입력 후 **실행** 가능
- 행 단위 **삭제** 버튼 제공 (주의: 즉시 반영)

> ⚠️ 잘못된 삭제는 복원이 어렵습니다. 신중하게 사용하세요.

---

## 13. PPT 내보내기

주간 보고 화면 우측 상단 **PPT 내보내기** 버튼을 클릭합니다.

### 내보내기 포함 내용

- **현재 선택된 주차**의 보고서
- 그룹별 슬라이드 자동 생성
- 비고 서식(굵게·색상·크기·밑줄·취소선) 반영
- 첨부파일 → 하이퍼링크로 삽입 (파일명 최대 15자 표시)

### 슬라이드 구성

| 컬럼 | 내용 |
|------|------|
| 업무 | 업무 항목 (등록 순서 유지) |
| Activity | 세부 활동 |
| 비고 | 서식 반영 텍스트 |
| 일정 | 예정 일정 |
| 상태 | 색상 구분 (완료:초록 / 진행중:노랑 / 예정:파랑) |
| 담당자 | 배정 사용자 |

### 첨부파일 하이퍼링크

- PPT에서 📎 링크 클릭 → 브라우저로 파일 다운로드
- **사내망 전용** (인증 없는 공개 엔드포인트 사용)

---

## 14. 계정 설정 (비밀번호 변경)

화면 우측 상단 로그인 정보 영역의 **비밀번호 변경** 버튼을 클릭합니다.

1. 현재 비밀번호 입력
2. 새 비밀번호 입력
3. 새 비밀번호 확인 입력
4. **변경** 클릭

> 관리자가 다른 사용자의 비밀번호를 초기화하려면 [사용자 관리](#9-사용자-관리-admin) 화면에서 수정합니다.

---

## 15. 환경 변수 (서버 설정)

서버 실행 시 아래 환경 변수로 동작을 제어할 수 있습니다.

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `SECRET_KEY` | `weekly-report-secret-key-2024` | JWT 서명 키 — **운영 시 반드시 변경** |
| `WR_DB_PATH` | `app_wr/app_wr.sqlite` | SQLite DB 파일 경로 |
| `API_URL` | 서버 자동 감지 | PPT 첨부 링크 기준 URL (예: `http://192.168.1.10:8000`) |

```bash
# 운영 환경 예시
export SECRET_KEY="your-strong-random-key-here"
export WR_DB_PATH="/data/app_wr.sqlite"
export API_URL="http://192.168.1.10:8000"
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

## 16. FAQ

**Q. 로그인이 안 됩니다.**
- 서버가 실행 중인지 확인하세요 (`uvicorn main:app --host 0.0.0.0 --port 8000`)
- 브라우저 주소창의 서버 IP·포트가 정확한지 확인하세요.
- 방화벽에서 해당 포트(기본 8000)가 허용되어 있는지 확인하세요.

**Q. 12시간 후 자동 로그아웃됩니다.**
- JWT 토큰 만료 시간입니다. 재로그인 후 계속 사용하세요.

**Q. 주간보고에서 내 팀/그룹이 자동 선택되지 않습니다.**
- 사용자 계정에 팀/그룹이 올바르게 설정되어 있는지 Admin에게 확인 요청하세요.

**Q. 전주 Activity를 불러왔는데 내용이 없습니다.**
- 전주에 해당 업무의 Activity가 등록되어 있어야 불러올 수 있습니다.

**Q. PPT 첨부 링크가 열리지 않습니다.**
- 사내망에서만 동작합니다. 외부망 또는 VPN 없이는 접근이 불가합니다.
- `API_URL` 환경 변수가 실제 서버 IP로 올바르게 설정되어 있는지 확인하세요.

**Q. 삭제된 Activity를 복원하고 싶습니다.**
- Admin 계정으로 로그인 → **🗑 휴지통** → **삭제된 Activity** 탭 → **복원** 클릭

**Q. DB 파일이 너무 커졌습니다.**
- **📎 첨부파일 관리** → 오래된 첨부파일 삭제 → **DB 압축하기** 클릭
